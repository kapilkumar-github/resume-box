import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const TemplateContainer = styled(Box)(() => ({

    width: 800,
    margin: '20px auto',
    padding: '10px',
    boxShadow: '0 0 3px 0px rgba(0,0,0,0.5)'
}));
